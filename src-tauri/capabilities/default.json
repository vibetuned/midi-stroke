{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "core APIs for the main window (event listeners for MIDI)",
  "windows": ["*"],
  "permissions": ["core:default"]
}
